# Leave unset for local dev — requests go through the Vite proxy to localhost:4000.
# In production (Vercel), set this to your deployed API's URL, e.g.:
# VITE_API_URL=https://meridian-crm-api.up.railway.app/api/v1
VITE_API_URL=
